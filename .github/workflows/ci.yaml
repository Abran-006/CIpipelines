name: ci-wf
on:
  pull_request:
    branches:
     - develop
    types:
     - opened 
     - synchronize
  push:
    branches:
      - develop
jobs:
  linting-job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome steps
        run: echo "welcome to Ci pipeline for this repo"